---
title: نقطة نهاية تبادل البيانات في تدفقات واتساب
date: 2026-06-24
category: whatsapp
description: Security and technical requirements for implementing the WhatsApp Flows Data Exchange Endpoint.
tags: [flows, data-exchange, encryption, timeout, validation]
---

# نقطة نهاية تبادل البيانات في تدفقات واتساب

يتطلب تنفيذ نقطة نهاية (Endpoint) آمنة وسريعة لتبادل البيانات في تدفقات واتساب (WhatsApp Flows) الالتزام الصارم ببروتوكولات التشفير وحدود المهلة الزمنية التي وضعتها شركة ميتا.

## تشفير وفك تشفير نقطة النهاية

تستخدم تدفقات واتساب قناة مشفرة من طرف إلى طرف.
- **البروتوكول**: يجب عليك تنفيذ بروتوكول تشفير واتساب للأعمال باستخدام **RSA** لتبادل المفاتيح و **AES-GCM** لتشفير حمولة البيانات (Payload).
- **إدارة المفاتيح**: قم بإنشاء زوج مفاتيح RSA بحجم 2048 بت. قم برفع المفتاح العام (Public Key) إلى حساب واتساب للأعمال الخاص بك (WABA) واحتفظ بالمفتاح الخاص (Private Key) بشكل آمن.
- **سير العمل**: قم بفك تشفير `encrypted_aes_key` باستخدام مفتاح RSA الخاص بك، ثم استخدم مفتاح AES ومتجه التهيئة (IV) لفك تشفير `encrypted_flow_data`. يجب تشفير استجابتك باستخدام نفس مفتاح AES ومتجه تهيئة معكوس.

## أقصى أحجام لحمولة البيانات (Payload)

على الرغم من عدم وجود حد أقصى دقيق للبتات موثق رسمياً، يوصى بشدة بالحفاظ على حجم حمولة البيانات في حده الأدنى.
- تجنب البيانات الكبيرة مثل الصور المرمزة بـ base64.
- قم بتضمين البيانات المحددة المطلوبة لمنطق التدفق فقط للحفاظ على زمن انتقال منخفض.

## التحققات من المهلة الزمنية (Timeouts)

يجب أن تقوم نقطة نهاية تبادل البيانات بمعالجة الطلب وإرجاع استجابة خلال **10 ثوانٍ**.
- إذا استغرق الخادم وقتاً أطول، تنتهي مهلة الطلب، مما يؤدي إلى ظهور خطأ "فشل في جلب الاستجابة" للمستخدم.
- تأكد من أداء المعالجة الثقيلة بشكل غير متزامن (Asynchronously) وتحسين استعلامات قاعدة البيانات.

## تحققات نقطة النهاية

<!-- preview -->
✅ Valid: تستجيب نقطة النهاية برمز 200 OK وحمولة JSON مشفرة وخفيفة خلال ثانيتين.
❌ Invalid: تتضمن نقطة النهاية صورة بحجم 5 ميجابايت مرمزة بـ base64 في حمولة البيانات وتستغرق 12 ثانية للمعالجة، مما يتسبب في خطأ انتهاء المهلة على تطبيق واتساب.

<!-- preview -->
✅ Valid: تنفذ نقطة النهاية معالجة مناسبة للأخطاء وتعيد شاشة خطأ محددة ومعينة إذا لم يتم العثور على معرف الحساب المطلوب.
❌ Invalid: تعتمد نقطة النهاية على أخطاء HTTP 500 بدون حمولات احتياطية مشفرة عند فشل التحقق.\n\n
<!-- panel:comparison -->
**حدث INIT مقابل حدث DATA_EXCHANGE**
- **حدث التهيئة (INIT):** يُرسل عند فتح الـ Flow. يُستخدم لجلب البيانات الديناميكية الأولية (مثل قائمة الفروع).
- **تبادل البيانات (DATA_EXCHANGE):** يُرسل عندما يرسل المستخدم شاشة أو يضغط على زر لجلب بيانات جديدة في منتصف الـ Flow.
<!-- endpanel -->
\n
<!-- panel:quiz -->
ماذا يحدث إذا استغرق الـ Endpoint الخاص بك أكثر من 10 ثوانٍ للرد؟
- [ ] يتلقى المستخدم رسالة واتساب عادية تخبره بالتأخير.
- [x] تنتهي مهلة الـ Flow (Timeout) ويظهر خطأ للمستخدم.
- [ ] يقوم واتساب بإعادة المحاولة 5 مرات تلقائياً.
- [ ] يستمر الـ Flow بشكل طبيعي بدون بيانات.
<!-- endpanel -->
\n