---
slug: whatsapp-campaigns-broadcasts
title: حملات وبث رسائل WhatsApp
platform: WhatsApp
category: Core API
priority: high
source_url: https://developers.facebook.com/docs/whatsapp/cloud-api/messages/message-templates
last_verified: 2026-06-24
---

# حملات وبث رسائل WhatsApp

في WhatsApp، لا يمكنك إرسال نصوص ترويجية حرة وعشوائية للمستخدمين متى شئت. يجب أن تبدأ أي محادثة يبدأها النشاط التجاري باستخدام **Message Template** (قالب رسالة) معتمد. هذا هو أساس البث (Broadcasts) والحملات (Campaigns) في SEEN V2.

## كيف يعمل البث

لإرسال بث إلى 1,000 مستخدم، يقوم SEEN V2 بالمرور على القائمة واستدعاء واجهة برمجة التطبيقات `/messages` باستخدام قالب معتمد مسبقاً لكل مستخدم. 

1. إنشاء قالب في Meta Business Manager (أو عبر API).
2. انتظار الموافقة (عادة من ثوانٍ إلى دقائق).
3. تنفيذ الحملة عن طريق إرسال القالب إلى الجمهور.

## فئات القوالب (تأثيرها على التسعير)

عند إنشاء القوالب، يجب تعيين فئة لها. تُسعر Meta المحادثات بناءً على هذه الفئة.

- **Marketing:** العروض الترويجية، الخصومات، النشرات الإخبارية. (الأعلى تكلفة)
- **Utility:** تحديثات المعاملات، تنبيهات الشحن، إشعارات الحساب.
- **Authentication:** كلمات المرور لمرة واحدة (OTPs)، رموز التحقق. (الأقل تكلفة)

## المعلمات والحقول (إرسال قالب)

| الحقل | النوع | مطلوب | الوصف |
|-------|------|----------|-------------|
| `type` | string | نعم | يتم تعيينه إلى `"template"` |
| `template.name` | string | نعم | الاسم الدقيق للقالب المعتمد |
| `template.language.code` | string | نعم | مثال: `"en_US"`, `"ar"` |
| `template.components` | array | لا | المتغيرات/الوسائط المعينة للعناصر النائبة في القالب |

### مصفوفة المكونات (The Components Array)

إذا كان قالبك يحتوي على متغيرات مثل `{{1}}` في النص (body)، أو يتطلب صورة في الترويسة (header)، فيجب عليك تمريرها في `components`.

| نوع المكون | النوع الفرعي | الوصف |
|----------------|----------|-------------|
| `"header"` | `image`, `video`, `document`, `text` | يملأ العنصر النائب في الترويسة |
| `"body"` | `text` | يستبدل `{{1}}`, `{{2}}` بالتسلسل |
| `"button"` | `payload`, `text` | الروابط الديناميكية (URLs) أو بيانات الرد السريع (Quick Reply payloads) |

## قواعد التحقق

| القاعدة | ❌ يتم الرفض | ✅ يتم القبول |
|------|-----------------|-----------------|
| مطابقة المتغيرات | القالب يحتوي على متغيرين، وتقوم بإرسال 1 | إرسال العدد المتوقع من المتغيرات بالضبط |
| رمز اللغة | `"en-us"` أو `"EN"` | تطابق تام `"en_US"` |
| حالة القالب | القالب بحالة `REJECTED` أو `PAUSED` | القالب بحالة `APPROVED` |
| قاعدة الـ 24 ساعة | *لا تنطبق على القوالب* | تتخطى القوالب نافذة خدمة العملاء البالغة 24 ساعة |

## معاينة حية — كيف تبدو

<!-- preview:accepted -->
### ✅ الاستخدام الصحيح — إرسال بث تسويقي

```json
POST /v19.0/{PHONE_NUMBER_ID}/messages
Authorization: Bearer {ACCESS_TOKEN}

{
  "messaging_product": "whatsapp",
  "recipient_type": "individual",
  "to": "{{CUSTOMER_PHONE}}",
  "type": "template",
  "template": {
    "name": "summer_sale_2026",
    "language": {
      "code": "en_US"
    },
    "components": [
      {
        "type": "header",
        "parameters": [
          {
            "type": "image",
            "image": {
              "link": "https://seen-v2.com/promo.jpg"
            }
          }
        ]
      },
      {
        "type": "body",
        "parameters": [
          { "type": "text", "text": "Sarah" },
          { "type": "text", "text": "50%" }
        ]
      }
    ]
  }
}
```

**لماذا تعمل:** يتوقع القالب `summer_sale_2026` ترويسة بصورة ومتغيرين في النص (الاسم والخصم). تقوم حمولة البيانات (payload) بتعيين هذه العناصر بشكل دقيق.

<!-- preview:rejected -->
### ❌ خطأ شائع — متغيرات المكونات مفقودة

```json
{
  "type": "template",
  "template": {
    "name": "order_update",
    "language": { "code": "en" }
  }
}
```

**الخطأ:** `Error 100: Invalid parameter` — إذا تمت الموافقة على `order_update` بنص "Your order {{1}} is arriving on {{2}}"، فإن إرساله بدون مصفوفة `components` سيؤدي إلى فشل التحقق على الفور.

## حدود جودة WhatsApp (صندوق العقوبات)

عند تنفيذ حملات ضخمة، تراقب Meta ملاحظات المستخدمين. إذا قام المستخدمون بحظر أو الإبلاغ عن البث الخاص بك:
1. **انخفاض المستوى (Tier Drop):** يمكن تخفيض حد المراسلة الخاص بك (على سبيل المثال، 10 آلاف/يوم) فوراً إلى 1000/يوم.
2. **إيقاف القالب مؤقتاً (Template Pause):** ستقوم Meta بوضع علامة `PAUSED` على القالب المحدد. سيؤدي إرساله إلى إرجاع خطأ حتى يتم إلغاء إيقافه مؤقتاً.
3. **حظر الرقم (Number Ban):** تؤدي الجودة المنخفضة المستمرة إلى تقييد كامل.

## ملاحظات تنفيذ SEEN V2

- **التجميع (Batching):** تقيد واجهة برمجة تطبيقات WhatsApp التزامن. بالنسبة لحملة تضم 100 ألف مستخدم، يجب أن يطبق SEEN V2 تحديد المعدل (بحد أقصى 80 طلباً في الثانية) لتجنب الخطأ HTTP 429 Too Many Requests.
- **الإخفاقات (Failures):** لا تمتلك جميع الأرقام تطبيق WhatsApp. يجب أن يتعامل SEEN V2 بشكل صحيح مع الخطأ `Error 131026: Message undeteliverable` ويضع علامة على جهة الاتصال وفقاً لذلك حتى يتم تخطيها في عمليات البث المستقبلية.

## قائمة التحقق من الجودة (QA)

- [ ] التأكد من أن SEEN V2 يقوم بتعيين العدد الصحيح من المتغيرات بالضبط لكل مكون
- [ ] فرض حساسية حالة الأحرف لرمز اللغة (`en_US` وليس `en-us`)
- [ ] تنفيذ نظام طابور (queue) لعمليات البث الكبيرة للالتزام بحدود المعدل (rate limits)
- [ ] قراءة حالات الـ webhook (`sent`، `delivered`، `read`، `failed`) لتحديث تحليلات الحملة في الوقت الفعلي
